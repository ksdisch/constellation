---
description: Deprecated alias — /moonshot is now the default "Moonshot" mode of the multi-mode /brainstorm command. Runs the same bold/visionary brainstorm (inverted twin of /autonomous-milestone) it always did. Prefer /brainstorm going forward; /brainstorm moonshot is the explicit form.
argument-hint: [optional seed — theme/horizon/leash]
---

Seed (optional): $ARGUMENTS

`/moonshot` was **renamed to `/brainstorm`** and is now one *mode* among several (Moonshot, QuickWin,
Subtract, Harden, Premortem, Friction, Delight, Positioning, Reach). This alias is kept so existing
muscle memory and older `docs/ideas/` references still work.

**Run `/brainstorm` in Moonshot mode now** — i.e. follow `.claude/commands/brainstorm.md` with the
mode fixed to **Moonshot** (the bold/visionary inverted twin of `/autonomous-milestone`): skip the
mode-pick step, load Moonshot's lenses + the Too-Timid ✕ Incoherent-Fantasy gate, and treat the seed
above as the `/brainstorm` seed. Everything else (Orient → Steer → Diverge → two-sided gate → Refine →
Synthesize → review-first Capture) is identical.

Mention once, briefly, that `/moonshot` is now `/brainstorm moonshot` — then proceed without further ceremony.
